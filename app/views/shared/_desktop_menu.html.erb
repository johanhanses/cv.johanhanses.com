<aside class="hidden lg:flex w-1/4 bg-dark text-white pt-12 flex-col items-end px-12">
  <div class="uppercase text-3xl mb-24 text-right">
    <p>Hej <%= Current.user.username ? Current.user.username : '' %></p> <%# dynamic username goes here%>
  </div>

  <nav class="uppercase text-xl space-y-6 text-right">
    <%= link_to 'Personligt brev', root_path, class: "hover:underline block" %>

    <%= link_to 'CV', cv_path, class: "hover:underline block" %>
  </nav>

  <nav class="text-sm text-right">
    <%= button_to 'Logga ut', destroy_user_session_path, method: :delete,  class: " uppercase hover:underline block mt-24" %>
  </nav>
</aside>
