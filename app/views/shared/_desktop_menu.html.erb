<aside class="hidden lg:flex w-1/4 bg-dark text-white pt-12 flex-col items-end px-12">
  <div class="uppercase text-3xl mb-24 text-right">
    <p>Hello <%= Current.user.username ? Current.user.username : '' %>!</p>
  </div>

  <nav class="uppercase text-xl space-y-6 text-right">
    <%= link_to 'Cover letter', root_path, class: class_names("hover:underline block", { active: current_page?(root_path) }) %>
    <%= link_to 'CV', cv_path, class: class_names("hover:underline block", { active: current_page?(cv_path) }) %>
    <%= link_to 'About', about_path, class: class_names("hover:underline block", { active: current_page?(about_path) }) %>
  </nav>

  <address class="mt-8 text-right">
    <a href="mailto:johanhanses@gmail.com" class="inline-block not-italic border border-white p-2 px-6 rounded-full hover:bg-white hover:text-dark transition-colors duration-300">johanhanses@gmail.com</a>
    <a href="tel:+46735506928" class="mt-4 inline-block not-italic border border-white p-2 px-6 rounded-full hover:bg-white hover:text-dark transition-colors duration-300">073-550 69 28</a>
  </address>

  <nav class="text-sm text-right">
    <%= button_to 'Log out', destroy_user_session_path, method: :delete,  class: " uppercase hover:underline block mt-24" %>
  </nav>
</aside>
